ClambHook commercial spec (durable): one-calendar-month trial from first launch; USD 99.99 one-time license incl. 1 year of all updates from purchase date; renew updates USD 9.99/yr (+1yr from max(cutoff,paymentDate)); usable at newest entitled version after cutoff; up to 10 concurrently active devices; checkout via Creem + NOWPayments ONLY (never PayPal). Three codebases under ~/projects/GitHub-JohnThre: clambercloud.com (landing + app UPDATE feeds), swiphtgroup.com (store: checkout/license/portal + now the installer DOWNLOAD), clambhook (Go/C app + ui/apple).

DISTRIBUTION SPLIT (corrected 2026-07-15, per user):
- End-user INSTALLER DOWNLOAD = swiphtgroup.com (store.swiphtgroup.com/clambhook/download/), NOT clambercloud.com.
- App UPDATE FEEDS stay on store.clambercloud.com: /api/clambhook/{update-manifest,appcast.xml}, the Sparkle app-download endpoint, and clambhook-release-key.asc. Shipped v1.0.0 app hardcodes store.clambercloud.com (Info.plist SUFeedURL, Settings.swift, release-macos.sh) so feeds must remain there.

IMPLEMENTED (built + verified, NOT yet deployed):
- swiphtgroup.com: added CLAMBHOOK_ARTIFACTS r2 binding (bucket clambhook-artifacts) in wrangler.toml (default+prod); new src/pages/api/clambhook/download.ts (R2-backed, asset=binary|sha256|sig, macOS DMG, default keys ClambhookMac-arm64.dmg[.sha256][.sha256.sig], content-types x-apple-diskimage/text-plain/pgp-signature, 503 when missing); new src/pages/clambhook/download.astro; clambhook-product.ts routes.download→/clambhook/download/ + added downloadApi + macOS/verify consts; env.d.ts typed CLAMBHOOK_MACOS_DMG_*_R2_KEY; env.example + wrangler prod vars; index.astro + buy.astro + license.astro download links now local. Both site builds pass; local pages-dev smoke: /clambhook/download/ 200, API 503 on empty local bucket (correct). Remote bucket confirmed has all 3 objects (sha256 matches manifest hash 3d02eef…947673).
- clambercloud.com: apps.ts CLAMBHOOK_DOWNLOAD_URL now ${STORE_URL}/clambhook/download/ (swiphtgroup); kept CLAMBHOOK_DOWNLOAD_STORE_URL + API_* feed consts on clambercloud. download.astro kept as info page (commercial-terms guardrail check-clambhook-commercial.mjs requires it) but all download CTAs → swiphtgroup; middleware store.clambercloud.com/ root 308→swiphtgroup download; geo-content.ts + support.astro + clambhook.astro + [...slug].astro structured-data downloadUrl → swiphtgroup. Build + commercial check pass; pages-dev smoke: page 200, CTAs → store.swiphtgroup.com, feeds still clambercloud.
- Cloudflare: ADDED store.clambercloud.com as custom domain on `clambercloud` Pages project (was missing — only clambercloud.com + www were attached; that absence was the original "download page not working" root cause AND broke shipped-app update checks). Status was initializing/pending; same-account zone auto-provisions DNS+cert.

OUTSTANDING (operator):
- DEPLOY swiphtgroup.com Pages (applies CLAMBHOOK_ARTIFACTS binding + new download page/route) — NOT done; deploys a live payment store, left to owner. clambercloud.com also needs redeploy for repointed CTAs.
- Confirm store.clambercloud.com custom domain reached active (cert issued).
- clambhook repo docs (README, docs/distribution.md, website-release/*, macos-release-contract-check.sh) still state end-user download from store.clambercloud.com — now stale; contract-check asserts that string. Separate policy-doc sweep needed if the split should be documented there.
- Payments providers Creem+NOWPayments verified working previously; PayPal excluded everywhere.
